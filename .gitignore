# Local .terraform directories
**/.terraform/*

# .tfstate files - NEVER commit these (contain sensitive data and state)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Allow example tfvars files to be committed
!*example.tfvars
!terraform.tfvars.example

# Ignore override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
*tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment variables
.env
.env.local
.env.*.local

# Backup files
*.backup
*.bak

# Log files
*.log

# SSH keys (if any)
*.pem
*.key

# Compressed files (if any temp archives)
*.zip
*.tar.gz

# Terraform workspace directories
terraform.tfstate.d/

# Terraform lock info file
.terraform.tfstate.lock.info

# Errored terraform state
errored.tfstate

# Terragrunt cache (if using Terragrunt)
.terragrunt-cache/
terragrunt-debug.tfvars.json

# Backend configuration (may contain sensitive data)
backend.hcl
*.backend.hcl

# Kubernetes config files
kubeconfig
kubeconfig.*
.kube/

# Secrets files
secrets.tf
*secrets*.tf

# Terraform plan binary output
*.tfplan
plan.out
plan.json

# Sentinel policies (if using Terraform Cloud/Enterprise)
*.sentinel

# Windows specific
Thumbs.db
ehthumbs.db
Desktop.ini

# macOS specific
.AppleDouble
.LSOverride
